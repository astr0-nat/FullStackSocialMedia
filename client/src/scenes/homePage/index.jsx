// keep data and components seperate is ideal
// .jsx just identifies it has react components in it, just a syntactic sugar
// so far we just set up this skeleton so we have a non-error when we import it

const HomePage = () => {
    return (<div>homepage</div>)
}
export default HomePage;
