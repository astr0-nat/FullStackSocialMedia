/* jsx is just to identify that this file has react components in it 
it is still a .js file */

const HomePage = () => {
    return (<div>homepage</div>)
}

export default HomePage;

// this is the skeleton just so we can have a non error when we import it